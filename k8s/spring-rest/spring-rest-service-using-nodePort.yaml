apiVersion: v1
kind: Service
metadata:
  name: spring-rest-service
  labels:
    app: spring-rest-app
spec:
  type: NodePort
  ports:
  - nodePort: 32000
    port: 9999
    targetPort: 8080
    protocol: TCP
  selector:
    app: spring-rest