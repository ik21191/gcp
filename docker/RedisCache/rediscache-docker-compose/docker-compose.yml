services:
  cache:
    image: redis:7.4-alpine
    container_name: my_rediscache
    restart: always
    ports:
      - "6379:6379"
    command: redis-server --save 20 1 --loglevel warning --requirepass mypass@321
    volumes:
      - cache:/data
volumes:
  cache:
    driver: local